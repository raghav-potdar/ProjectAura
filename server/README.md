# Aura Server

This small Express server provides a `/api/register` endpoint backed by SQLite.

Quick start:

1. cd server
2. npm install
3. npm start

Server runs on port 4000 by default and stores data in `server/data.sqlite`.
